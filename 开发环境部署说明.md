# 开发环境部署说明

> 生成时间：2026年8月6日
> 系统：Ubuntu 24.04（中文环境）
> 用途：Flutter 安卓 App 开发

## 一、开发工具位置

| 工具 | 位置 | 大小 | 用途 |
|------|------|------|------|
| Flutter SDK | /home/zhang/flutter_sdk | 2.3G | 核心开发框架（3.44.8 稳定版） |
| Android SDK | /home/zhang/development/android-sdk | 3.0G | 安卓编译打包（platform 36 + build-tools + NDK + CMake） |
| JDK 17 | /home/zhang/development/jdk17 | 318M | Java 运行环境（Gradle/构建需要） |
| Gradle 缓存 | /home/zhang/.gradle | 2.4G | Gradle 发行版 + 全部 Maven 依赖（已下完，新项目复用） |
| Pub 包缓存 | /home/zhang/.pub-cache | 647M | Flutter 第三方包（Riverpod/drift 等已缓存） |
| Git | 系统自带 /usr/bin/git | - | 版本管理 |

## 二、环境变量配置（~/.bashrc，开终端自动生效）

```bash
export JAVA_HOME=$HOME/development/jdk17
export ANDROID_HOME=$HOME/development/android-sdk
export FLUTTER_HOME=$HOME/flutter_sdk
export PATH=$FLUTTER_HOME/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin:$JAVA_HOME/bin:$PATH
# Flutter 国内镜像（加速组件下载）
export PUB_HOSTED_URL=https://pub.flutter-io.cn
export FLUTTER_STORAGE_BASE_URL=https://storage.flutter-io.cn
```

> 注意：.bashrc 是新终端才加载。已开的终端若要用 flutter 命令，需 source ~/.bashrc 或重开终端。

## 三、镜像加速配置（国内网络必备）

| 配置点 | 文件 | 加速内容 |
|--------|------|---------|
| Flutter 组件 | ~/.bashrc 两个环境变量 | 引擎/资源走 flutter-io.cn |
| Gradle 发行版 | android/gradle/wrapper/gradle-wrapper.properties | 腾讯云镜像（gradle-9.1.0-all.zip） |
| Maven 依赖 | android/settings.gradle.kts + build.gradle.kts | 阿里云镜像（google/mavenCentral） |

```gradle
// android/settings.gradle.kts 中的镜像写法示例
maven { url = uri("https://maven.aliyun.com/repository/google") }
maven { url = uri("https://maven.aliyun.com/repository/public") }
```

## 四、项目与产物位置

| 项 | 位置 |
|----|------|
| 项目源码 | /home/zhang/development/（lib/ android/ test/） |
| Git 版本库 | /home/zhang/development/.git（已提交 v0.1 / v0.1.1） |
| APK 成品 | /home/zhang/桌面/铁路运转日志-v1.0.apk（56M） |
| 构建输出 | /home/zhang/development/build/app/outputs/flutter-apk/ |

## 五、常用命令

```bash
flutter create my_app          # 创建新项目
flutter run                    # 运行（需连设备/模拟器）
flutter build apk --release    # 打正式安装包
flutter analyze                # 静态检查代码
flutter test                   # 运行自动化测试
flutter pub add 包名            # 添加依赖包
git add -A && git commit -m "msg"   # 提交版本
```

## 六、重要注意事项

1. 目录混用说明：项目源码和 Android SDK / JDK 同在 /home/zhang/development 下，
   已用 .gitignore 排除工具目录，不会污染版本库。
2. Flutter SDK 的 git remote 当前指向无效地址（为跳过版本检查），
   不影响开发；执行 flutter upgrade 前需恢复为官方源。
3. 首次构建的坑：drift 依赖的 sqlite3 包要从 GitHub 下载原生库，
   本机已把 4 个架构的库（arm64/arm/x64/ia32）放入构建缓存，
   哈希校验通过。若缓存被清理，需用加速域名 gh.logchenz.qzz.io 重新下载。
4. GitHub 加速域名：gh.logchenz.qzz.io（替换 github.com，路径不变）。
5. 磁盘占用约 8.5G，剩余空间充足，可再开多个 Flutter 项目。

## 七、技术栈速查

| 技术 | 说明 |
|------|------|
| Flutter 3.44.8 + Dart 3.12.2 | 开发框架
| Riverpod 2.6 | 状态管理
| drift 2.34 | 本地数据库（SQLite）
| go_router 17 | 页面导航
| intl | 日期格式化
| Material 3 | UI 设计规范

---
本文档由 AI 协助生成，保存于 2026年8月6日
