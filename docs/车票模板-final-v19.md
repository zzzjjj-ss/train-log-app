# 车票模板 - 最终版 v19（用户已确认）

> **状态：已获用户确认 "简直完美"（2026-08-13）。后续 CustomPainter 实现以此为准，勿再改结构。**
> 参考图：`docs/车票模板-final-v19.png`（透明背景 PNG）

---

## 一、用户提供的原始 path（坐标基础）

```svg
# path1 弧线（原样使用，贝塞尔）
M160.82 146.64 C103.61 134.62 80 130 60 130 C27.75 130.7 23.53 150.48 31.44 165.79

# path2 票面轮廓（已修成横平竖直矩形，不加圆角前）
M10.57 93.01 L161.75 95.57 L162.48 178.59 L9.25 180.05 L10.13 92.14

# path3 色带顶线（已修成平线 y167）
M10.57 167.18 L161.53 166.3

# path4 右侧连接条（用户手绘参考，最终已统一尺寸）
M164.4 102.68 L164.18 116.11
M164.69 154.58 L165.36 166.03
```

---

## 二、最终结构（viewBox 0 0 170 190）

### 1. 票面
- 矩形 `(9,93)-(162,180)`，横平竖直
- **四角圆角 `r=6`**
- 填充浅蓝 `rgb(147,204,227)`

### 2. 弧面（关键闭合方式）
- 弧线 = path1 两段贝塞尔：
  - 第1段：p0`(160.82,146.64)` c1`(103.61,134.62)` c2`(80,130)` p3`(60,130)`
  - 第2段：p0`(60,130)` c1`(27.75,130.7)` c2`(23.53,150.48)` p3`(31.44,165.79)`
- **闭合 polygon（不能错）**：
  ```
  curve(贝塞尔点集) + [(31.44, 167), (162, 167), (162, 146.64)]
  ```
  （弧线 → 左端向下到 y167 → 横线到最右 x162 → 沿右缘向上闭合）
- 填充中浅蓝 `rgb(116,190,223)`

### 3. 底部色带
- 横条 `y167-180`，横平竖直
- **顶部两角直角**（平线 y167）
- **底部两角圆角 `r=6`**（与票面圆角弧度统一）
- 填充深蓝 `rgb(66,153,190)`

### 4. 连接条（出票连接条，左右各上下一个，共4个）
- **统一尺寸**：高 `12`、凸出 `2`（从票面边缘向外）
- **无描边**
- **颜色 = 接合处颜色**：
  - 右上 / 左上 / 左下 → 浅蓝（接票面）
  - 右下 → 中浅蓝（接弧面）
- 位置：
  - 上方两个：`y 102.68-114.68`
  - 下方两个：`y 154.58-166.58`
  - 右侧：`x 162-164`；左侧：`x 7-9`

### 5. 背景
- **透明**（PNG RGBA）

---

## 三、关键决策记录（防回退）

| 项 | 结论 |
|----|------|
| 票面形状 | 横平竖直矩形 + 圆角 r6，**不要手绘不规则** |
| 弧面闭合 | 弧线→y167→最右→右缘向上闭合，**不是两端水平直闭** |
| 色块顶部 | **直角**，底部圆角与票面统一 |
| 连接条 | 小窄条凸出 2px，统一尺寸，无打孔无描边，颜色=接合处 |

---

## 四、可运行参考实现

见同目录 `render_ticket_v19.py`（Python/PIL，生成透明背景 PNG）。
